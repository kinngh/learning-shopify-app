const customers_marketing_consent_update_handler = async (
  topic,
  shop,
  webhookRequestBody
) => {
  console.log(topic);
  console.log(webhookRequestBody);
};
module.exports = customers_marketing_consent_update_handler;
