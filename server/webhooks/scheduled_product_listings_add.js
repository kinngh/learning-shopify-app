const scheduled_product_listings_add_handler = async (
  topic,
  shop,
  webhookRequestBody
) => {
  console.log(topic);
  console.log(webhookRequestBody);
};
export default scheduled_product_listings_add_handler;
