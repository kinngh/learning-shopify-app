const app_subscriptions_approaching_capped_amount_handler = async (
  topic,
  shop,
  webhookRequestBody
) => {
  console.log(topic);
  console.log(webhookRequestBody);
};
module.exports = app_subscriptions_approaching_capped_amount_handler;
