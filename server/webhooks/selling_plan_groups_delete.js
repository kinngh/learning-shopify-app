const selling_plan_groups_delete_handler = async (
  topic,
  shop,
  webhookRequestBody
) => {
  console.log(topic);
  console.log(webhookRequestBody);
};
module.exports = selling_plan_groups_delete_handler;
